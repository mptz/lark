|* Hard-to-reduce terms of various flavors.

|* Tricky open term
I := [x. x].
D := [x. x; x].
F := [f. #y; f; f].
I; F; D.

|* Asperti & Guerrini p.12, reduces to I
AG12-R := [g. I; g; g].
AG12-S := [h. I; h; AG12-R]; AG12-R.
AG12-S.

|* Keyword vs bound vs free variable tests
let {let := 7} let + let.
let {let := 7} let {let := 5} let + let.
let := #let.
$conceal let.
let {let := let} let.
